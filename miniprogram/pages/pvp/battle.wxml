<custom-nav title="战报回放" theme="dark"></custom-nav>
<view class="container redirect-page">
  <view wx:if="{{redirecting}}" class="redirect-state">战斗画面加载中...</view>
  <view wx:elif="{{error}}" class="redirect-state">{{error}}</view>
  <view wx:else class="redirect-state">战斗画面已迁移，请返回上一页重试</view>
</view>

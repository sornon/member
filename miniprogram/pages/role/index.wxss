page {
  background: #050921;
  min-height: 100%;
}

.page {
  padding: 0 32rpx;
  min-height: 100%;
  display: flex;
  flex-direction: column;
  box-sizing: border-box;
  color: #e5ecff;
}

.tab-bar {
  display: flex;
  justify-content: space-around;
  align-items: center;
  margin: 0;
  margin-bottom: 24rpx;
  height: 96rpx;
  box-sizing: border-box;
}

.tab-item {
  flex: 1;
  display: flex;
  align-items: center;
  justify-content: center;
  text-align: center;
  font-size: 28rpx;
  color: rgba(167, 186, 255, 0.72);
  position: relative;
  padding: 20rpx 0;
  transition: color 0.2s ease;
  line-height: 1;
}

.tab-item::after {
  content: '';
  position: absolute;
  left: 50%;
  bottom: 12rpx;
  transform: translateX(-50%);
  width: 48rpx;
  height: 6rpx;
  border-radius: 999rpx;
  background: transparent;
  transition: background 0.2s ease;
}

.tab-item--active {
  color: #f5f7ff;
  font-weight: 600;
}

.tab-item--active::after {
  background: linear-gradient(90deg, #566aff, #8a4bff);
}

.tab-item--hover {
  color: rgba(212, 222, 255, 0.9);
}

.loading {
  padding: 200rpx 0;
  text-align: center;
  color: rgba(189, 201, 255, 0.75);
  font-size: 28rpx;
}

.scroll-container {
  flex: 1;
  box-sizing: border-box;
}

.section-card {
  background: rgba(15, 25, 60, 0.92);
  border-radius: 28rpx;
  padding: 32rpx;
  margin-bottom: 32rpx;
  box-shadow: 0 18rpx 36rpx rgba(4, 8, 28, 0.55);
  border: 1rpx solid rgba(90, 118, 255, 0.24);
}

.role-attributes {
  background: transparent;
  border: none;
  box-shadow: none;
  padding: 12rpx 0 24rpx;
}

.section-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 24rpx;
}

.section-title {
  font-size: 32rpx;
  font-weight: 700;
  color: #f3f6ff;
}

.section-actions {
  display: flex;
  align-items: center;
  gap: 20rpx;
}

.section-actions--skill {
  flex-direction: column;
  align-items: flex-end;
  gap: 12rpx;
}

.section-actions--skill .section-actions__meta {
  text-align: right;
  white-space: nowrap;
}

.skill-action-buttons {
  display: flex;
  gap: 16rpx;
  flex-wrap: wrap;
  justify-content: flex-end;
}

.section-actions__info {
  font-size: 24rpx;
  color: rgba(189, 203, 255, 0.82);
}

.section-actions__button {
  min-width: 140rpx;
}

.section-actions__meta {
  display: flex;
  align-items: center;
  font-size: 24rpx;
  color: rgba(198, 210, 255, 0.78);
  white-space: nowrap;
}

.action-btn,
.pill-btn {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  line-height: 1;
  padding: 16rpx 28rpx;
  border-radius: 999rpx;
  border: 1rpx solid rgba(108, 136, 255, 0.32);
  background: rgba(60, 88, 190, 0.18);
  color: #e6edff;
  font-size: 24rpx;
  font-weight: 600;
  min-width: 140rpx;
  box-sizing: border-box;
  margin: 0;
  transition: background 0.2s ease, box-shadow 0.2s ease, color 0.2s ease,
    border-color 0.2s ease;
}

.action-btn::after,
.pill-btn::after {
  display: none;
}

.action-btn--hover,
.pill-btn--hover {
  background: rgba(84, 112, 220, 0.28);
  border-color: rgba(138, 162, 255, 0.48);
  color: #f3f6ff;
}

.action-btn--primary,
.pill-btn--primary {
  background: linear-gradient(120deg, rgba(102, 132, 255, 0.95), rgba(140, 91, 255, 0.95));
  border-color: rgba(140, 159, 255, 0.72);
  color: #f7f9ff;
  box-shadow: 0 12rpx 28rpx rgba(88, 110, 230, 0.32);
}

.action-btn--primary-hover,
.pill-btn--primary-hover {
  background: linear-gradient(120deg, rgba(116, 145, 255, 0.98), rgba(150, 100, 255, 0.98));
  border-color: rgba(158, 176, 255, 0.82);
  box-shadow: 0 16rpx 32rpx rgba(96, 120, 240, 0.38);
}

.action-btn--ghost,
.pill-btn--ghost {
  background: rgba(54, 80, 172, 0.2);
  border-color: rgba(115, 142, 255, 0.34);
  color: #dae3ff;
}

.pill-btn--danger {
  background: linear-gradient(120deg, rgba(250, 81, 81, 0.92), rgba(255, 138, 84, 0.92));
  border-color: rgba(255, 150, 150, 0.72);
  color: #fff7f7;
  box-shadow: 0 12rpx 28rpx rgba(255, 98, 98, 0.28);
}

.pill-btn--danger-hover {
  background: linear-gradient(120deg, rgba(255, 104, 104, 0.96), rgba(255, 160, 110, 0.96));
  border-color: rgba(255, 178, 178, 0.82);
  box-shadow: 0 16rpx 36rpx rgba(255, 120, 120, 0.34);
}

.pill-btn--enhance {
  background: linear-gradient(120deg, rgba(88, 212, 186, 0.95), rgba(72, 164, 248, 0.95));
  border-color: rgba(122, 226, 210, 0.72);
  color: #f6fffb;
  box-shadow: 0 12rpx 28rpx rgba(74, 198, 192, 0.32);
}

.pill-btn--enhance-hover {
  background: linear-gradient(120deg, rgba(104, 224, 198, 0.98), rgba(92, 180, 255, 0.98));
  border-color: rgba(148, 236, 220, 0.82);
  box-shadow: 0 16rpx 36rpx rgba(92, 206, 210, 0.38);
}

button.action-btn[disabled],
button.pill-btn[disabled] {
  opacity: 0.58;
  background: rgba(48, 70, 152, 0.22);
  border-color: rgba(104, 130, 238, 0.24);
  color: rgba(205, 214, 255, 0.68);
  box-shadow: none;
}


.attr-list {
  margin-top: 12rpx;
  display: grid;
  grid-template-columns: repeat(2, minmax(0, 1fr));
  gap: 12rpx 16rpx;
}

.attr-row {
  display: flex;
  align-items: center;
  gap: 20rpx;
  font-size: 26rpx;
  color: rgba(215, 224, 255, 0.88);
}

.attr-row__label {
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
  font-size: 30rpx;
  font-weight: 600;
  color: rgba(192, 205, 255, 0.85);
  flex-shrink: 0;
}

.attr-row__value {
  font-size: 24rpx;
  font-weight: 500;
  color: #f1f4ff;
  min-width: 50rpx;
  text-align: left;
}

.attr-row__controls {
  display: inline-flex;
  align-items: center;
  gap: 8rpx;
}

.attr-control {
  width: 44rpx!important;
  height: 44rpx;
  border-radius: 50%;
  border: 1rpx solid rgba(103, 128, 228, 0.6);
  background: rgba(18, 28, 70, 0.85);
  color: #dfe6ff;
  font-size: 28rpx;
  line-height: 44rpx;
  text-align: center;
  padding: 0;
}

.attr-control::after {
  display: none;
}

.attr-control--minus {
  color: rgba(220, 164, 182, 0.92);
  border-color: rgba(220, 164, 182, 0.52);
}

.attr-control--plus {
  color: rgba(164, 214, 220, 0.92);
  border-color: rgba(164, 214, 220, 0.52);
}

.attr-input {
  width: 50rpx;
  height: 44rpx;
  border-radius: 12rpx;
  border: 1rpx solid rgba(103, 128, 228, 0.6);
  background: rgba(10, 16, 40, 0.9);
  color: #f5f7ff;
  font-size: 24rpx;
  text-align: center;
  padding: 0 10rpx;
}

.attr-input::placeholder {
  color: rgba(168, 184, 241, 0.5);
}

.attr-extra {
  margin-top: 24rpx;
  display: flex;
  justify-content: space-between;
  color: rgba(168, 184, 241, 0.78);
  font-size: 24rpx;
}

.attr-extra__remaining {
  position: relative;
  display: inline-flex;
  align-items: center;
  padding-right: 22rpx;
}

.attr-extra__dot {
  position: absolute;
  top: 0;
  right: 0;
  transform: translate(45%, -55%);
}

.attr-actions {
  margin-top: 20rpx;
  display: flex;
  justify-content: flex-end;
  align-items: center;
  gap: 20rpx;
  flex-wrap: wrap;
}

.attr-actions__button {
  min-width: 150rpx;
}

.subsection-title {
  margin-top: 24rpx;
  font-size: 32rpx;
  font-weight: 700;
  color: #f3f6ff;
}

.combat-grid {
  margin-top: 12rpx;
  display: grid;
  grid-template-columns: repeat(5, minmax(0, 1fr));
  gap: 10rpx 8rpx;
}

.combat-item {
  display: flex;
  flex-direction: column;
  gap: 4rpx;
  color: rgba(214, 224, 255, 0.85);
}

.combat-label {
  font-size: 26rpx;
  font-weight: 600;
  color: rgba(190, 204, 255, 0.72);
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}

.combat-value {
  font-size: 24rpx;
  color: #f1f4ff;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}


.slot-grid {
  display: grid;
  grid-template-columns: repeat(4, minmax(0, 1fr));
  gap: 16rpx;
}

.slot-card {
  position: relative;
  background: rgba(22, 36, 86, 0.9);
  border-radius: 20rpx;
  padding: 18rpx 16rpx 24rpx;
  border: 1rpx solid rgba(93, 119, 214, 0.36);
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 12rpx;
  min-height: 196rpx;
  box-sizing: border-box;
}

.slot-card--empty .slot-card__icon {
  color: rgba(167, 186, 255, 0.68);
  border-color: rgba(94, 120, 210, 0.4);
}

.slot-card__header {
  width: 100%;
  display: flex;
  justify-content: center;
  font-size: 22rpx;
  color: rgba(190, 204, 255, 0.86);
  font-weight: 600;
}

.slot-card__slot {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  line-height: 1;
}

.slot-card__icon {
  width: 128rpx;
  height: 128rpx;
  border: 4rpx solid rgba(113, 140, 255, 0.6);
  border-radius: 20rpx;
  background: rgba(12, 20, 52, 0.9);
  color: #f5f8ff;
  position: relative;
  overflow: hidden;
  box-sizing: border-box;
}

.slot-card__icon-image {
  width: 100%;
  height: 100%;
  display: block;
}

.slot-card__icon-label {
  position: absolute;
  left: 0;
  right: 0;
  bottom: 0;
  padding: 10rpx;
  font-size: 18rpx;
  font-weight: 600;
  text-align: center;
  line-height: 1.3;
  color: #f5f8ff;
  background: linear-gradient(180deg, rgba(10, 16, 40, 0) 0%, rgba(10, 16, 40, 0.88) 100%);
  box-sizing: border-box;
  z-index: 1;
}

.slot-card__icon-placeholder {
  width: 100%;
  height: 100%;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 18rpx;
  font-weight: 600;
}

.inventory-title {
  margin: 32rpx 0 16rpx;
  font-size: 26rpx;
  color: rgba(186, 200, 255, 0.8);
  font-weight: 600;
}

.inventory-empty {
  margin-bottom: 12rpx;
}

.inventory-grid {
  display: grid;
  grid-template-columns: repeat(4, minmax(0, 1fr));
  gap: 16rpx;
}

.inventory-card {
  position: relative;
  background: rgba(18, 30, 74, 0.9);
  border-radius: 20rpx;
  border: 1rpx solid rgba(84, 110, 208, 0.34);
  box-sizing: border-box;
}

.inventory-card::before {
  content: '';
  display: block;
  padding-top: 100%;
}

.inventory-card__frame {
  position: absolute;
  inset: 12rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 1;
}

.inventory-card__badge {
  position: absolute;
  top: 12rpx;
  left: 12rpx;
  padding: 4rpx 10rpx;
  font-size: 20rpx;
  border-radius: 999rpx;
  background: rgba(70, 90, 190, 0.4);
  color: rgba(214, 224, 255, 0.88);
  display: inline-flex;
  align-items: center;
  justify-content: center;
  line-height: 1;
  z-index: 2;
}

.inventory-card__icon {
  width: 100%;
  height: 100%;
  border: 4rpx solid rgba(120, 146, 255, 0.62);
  border-radius: 16rpx;
  background: rgba(8, 14, 40, 0.9);
  color: #f5f8ff;
  box-sizing: border-box;
  position: relative;
  overflow: hidden;
}

.inventory-card__icon-image {
  width: 100%;
  height: 100%;
  display: block;
}

.inventory-card__icon-label {
  position: absolute;
  left: 0;
  right: 0;
  bottom: 0;
  padding: 10rpx;
  font-size: 18rpx;
  font-weight: 600;
  text-align: center;
  line-height: 1.3;
  color: #f5f8ff;
  background: linear-gradient(180deg, rgba(6, 12, 32, 0) 0%, rgba(6, 12, 32, 0.92) 100%);
  box-sizing: border-box;
  z-index: 1;
}

.inventory-card__status {
  position: absolute;
  right: 12rpx;
  bottom: 12rpx;
  padding: 4rpx 10rpx;
  font-size: 18rpx;
  border-radius: 999rpx;
  background: rgba(10, 22, 56, 0.72);
  color: rgba(236, 246, 255, 0.9);
  display: inline-flex;
  align-items: center;
  justify-content: center;
  line-height: 1;
  font-weight: 600;
  z-index: 2;
}

.inventory-card--equipped {
  border-color: rgba(132, 178, 255, 0.62);
  box-shadow: 0 12rpx 28rpx rgba(72, 112, 220, 0.24);
}

.inventory-card--equipped .inventory-card__icon {
  box-shadow: 0 0 0 4rpx rgba(120, 176, 255, 0.32);
}

.storage-section {
  position: relative;
}

.storage-tabs {
  display: flex;
  flex-wrap: wrap;
  gap: 16rpx;
  margin-bottom: 24rpx;
}

.storage-tab {
  padding: 14rpx 24rpx;
  border-radius: 999rpx;
  border: 1rpx solid rgba(86, 112, 220, 0.32);
  background: rgba(20, 34, 84, 0.62);
  color: rgba(198, 210, 255, 0.82);
  font-size: 24rpx;
  display: inline-flex;
  align-items: center;
  gap: 10rpx;
  line-height: 1;
  position: relative;
  transition: all 0.2s ease;
}

.storage-tab__label {
  font-weight: 600;
}

.storage-tab__dot {
  position: absolute;
  top: -4rpx;
  right: -4rpx;
}

.storage-tab--active {
  border-color: rgba(130, 156, 255, 0.6);
  background: linear-gradient(120deg, rgba(94, 124, 248, 0.92), rgba(144, 94, 255, 0.92));
  color: #f8faff;
  box-shadow: 0 10rpx 22rpx rgba(86, 108, 228, 0.28);
}

.storage-summary {
  display: flex;
  flex-direction: column;
  gap: 16rpx;
  padding: 20rpx 24rpx;
  border-radius: 24rpx;
  background: rgba(24, 38, 88, 0.72);
  border: 1rpx solid rgba(94, 122, 230, 0.28);
  margin-bottom: 28rpx;
}

.storage-summary__line {
  display: flex;
  justify-content: space-between;
  font-size: 24rpx;
  color: rgba(205, 214, 255, 0.82);
  flex-wrap: wrap;
  gap: 8rpx 24rpx;
}

.storage-summary__progress {
  width: 100%;
}

.storage-summary__actions {
  display: flex;
  align-items: center;
  gap: 16rpx;
  flex-wrap: wrap;
}

.storage-summary__upgrade {
  min-width: auto;
  padding: 12rpx 24rpx;
  font-size: 22rpx;
}

.storage-summary__hint {
  font-size: 22rpx;
  color: rgba(186, 198, 255, 0.76);
}

.storage-grid {
  display: grid;
  grid-template-columns: repeat(4, minmax(0, 1fr));
  gap: 16rpx;
}

.storage-slot {
  position: relative;
  background: rgba(16, 28, 70, 0.85);
  border-radius: 20rpx;
  border: 1rpx solid rgba(86, 116, 210, 0.34);
  box-sizing: border-box;
  min-height: 0;
  overflow: visible;
}

.storage-slot--has-icon {
  background: transparent;
  border: none;
}

.storage-slot::before {
  content: '';
  display: block;
  padding-top: 100%;
}

.storage-slot__badge {
  position: absolute;
  top: 12rpx;
  left: 12rpx;
  padding: 4rpx 12rpx;
  font-size: 20rpx;
  border-radius: 999rpx;
  background: rgba(72, 94, 200, 0.42);
  color: rgba(214, 224, 255, 0.88);
  line-height: 1;
  z-index: 2;
}

.storage-slot__recommend {
  position: absolute;
  top: 8rpx;
  right: 8rpx;
  width: 44rpx;
  height: 44rpx;
  border-radius: 999rpx;
  background: rgba(46, 184, 123, 0.92);
  display: flex;
  align-items: center;
  justify-content: center;
  box-shadow: 0 6rpx 14rpx rgba(46, 184, 123, 0.38);
  pointer-events: none;
  z-index: 3;
}

.storage-slot__recommend-icon {
  font-size: 28rpx;
  color: #f5fff8;
  font-weight: 700;
  transform: translateY(-2rpx);
}

.storage-slot__flag {
  position: absolute;
  top: -6rpx;
  right: -6rpx;
  padding: 4rpx 18rpx;
  border-radius: 999rpx;
  background: linear-gradient(135deg, #ff6b6b, #ff8a5c);
  color: #ffffff;
  font-size: 20rpx;
  font-weight: 700;
  line-height: 1;
  box-shadow: 0 8rpx 16rpx rgba(255, 107, 107, 0.35);
  z-index: 4;
  pointer-events: none;
}

.storage-slot__frame {
  position: absolute;
  inset: 12rpx;
  border-radius: 16rpx;
  border: 4rpx solid rgba(118, 146, 250, 0.58);
  background: rgba(10, 18, 48, 0.9);
  display: flex;
  align-items: center;
  justify-content: center;
  box-sizing: border-box;
  transition: box-shadow 0.2s ease, transform 0.2s ease, border-color 0.2s ease;
  overflow: visible;
}

.storage-slot__frame--icon {
  inset: 0;
  border: none;
  background: transparent;
  border-radius: 20rpx;
}

.storage-slot__frame--hover {
  border-color: rgba(150, 176, 255, 0.68);
  box-shadow: 0 10rpx 24rpx rgba(90, 118, 240, 0.32);
  transform: translateY(-2rpx);
}

.storage-slot__frame--empty {
  border: 2rpx dashed rgba(102, 128, 210, 0.32);
  background: rgba(12, 18, 40, 0.65);
}

.storage-slot__icon {
  width: 100%;
  height: 100%;
  border: 4rpx solid rgba(118, 146, 250, 0.58);
  border-radius: 16rpx;
  background: rgba(12, 18, 40, 0.9);
  color: #f4f6ff;
  position: relative;
  overflow: hidden;
  box-sizing: border-box;
}

.storage-slot__icon--image {
  border: none;
  background: transparent;
  border-radius: 20rpx;
}

.storage-slot__icon-image {
  width: 100%;
  height: 100%;
  display: block;
  border-radius: inherit;
}

.storage-slot__name {
  font-size: 20rpx;
  font-weight: 600;
  text-align: center;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}

.storage-slot__name--overlay {
  position: absolute;
  left: 0;
  right: 0;
  bottom: 0;
  padding: 10rpx;
  color: #f4f6ff;
  background: linear-gradient(180deg, rgba(8, 14, 34, 0) 0%, rgba(8, 14, 34, 0.92) 100%);
  box-sizing: border-box;
  z-index: 1;
}

.storage-slot__name--placeholder {
  width: 100%;
  height: 100%;
  display: flex;
  align-items: center;
  justify-content: center;
}

.storage-slot__placeholder {
  font-size: 22rpx;
  color: rgba(168, 186, 255, 0.6);
  display: flex;
  align-items: center;
  justify-content: center;
  width: 100%;
  height: 100%;
}

.storage-slot--empty {
  border: 1rpx dashed rgba(94, 122, 210, 0.32);
  background: rgba(14, 22, 48, 0.6);
}

.storage-empty {
  margin-top: 24rpx;
}

.equipment-summary {
  margin-top: 28rpx;
  padding: 24rpx;
  border-radius: 24rpx;
  background: rgba(30, 46, 98, 0.78);
  border: 1rpx solid rgba(100, 130, 240, 0.28);
  display: flex;
  flex-direction: column;
  gap: 12rpx;
}

.summary-title {
  font-size: 26rpx;
  color: #f4f6ff;
  font-weight: 600;
}

.set-item {
  border-radius: 20rpx;
  padding: 18rpx 22rpx;
  background: rgba(18, 30, 74, 0.62);
  border: 1rpx solid rgba(96, 124, 228, 0.24);
  display: flex;
  flex-direction: column;
  gap: 10rpx;
}

.set-head {
  display: flex;
  justify-content: space-between;
  align-items: center;
  font-size: 24rpx;
  color: rgba(217, 226, 255, 0.92);
  font-weight: 600;
}

.set-count {
  font-size: 22rpx;
  color: rgba(179, 196, 255, 0.82);
}

.set-effect {
  font-size: 22rpx;
  color: rgba(197, 210, 255, 0.82);
  line-height: 1.5;
}

.note-item {
  font-size: 22rpx;
  color: rgba(197, 210, 255, 0.82);
  line-height: 1.5;
  padding: 10rpx 12rpx;
  border-radius: 16rpx;
  background: rgba(20, 32, 76, 0.58);
  border: 1rpx solid rgba(92, 118, 225, 0.2);
}

.inventory-footer {
  margin-top: 20rpx;
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 18rpx 24rpx;
  border-radius: 20rpx;
  background: linear-gradient(135deg, rgba(79, 120, 255, 0.18), rgba(133, 94, 255, 0.18));
  color: #f1f4ff;
  transition: transform 0.2s ease, box-shadow 0.2s ease;
  border: 1rpx solid rgba(90, 118, 255, 0.24);
}

.inventory-footer--hover {
  transform: translateY(-2rpx);
  box-shadow: 0 12rpx 28rpx rgba(76, 96, 190, 0.24);
}

.inventory-footer__info {
  display: flex;
  align-items: center;
  gap: 12rpx;
}

.inventory-footer__icon {
  font-size: 36rpx;
}

.inventory-footer__amount {
  font-size: 30rpx;
  font-weight: 700;
}

.inventory-footer__label {
  font-size: 24rpx;
  color: rgba(197, 208, 255, 0.8);
}

.equipment-tooltip {
  position: fixed;
  inset: 0;
  z-index: 999;
  display: flex;
  align-items: center;
  justify-content: center;
}

.equipment-tooltip__mask {
  position: absolute;
  inset: 0;
  background: rgba(5, 9, 28, 0.72);
}

.equipment-tooltip__container {
  position: relative;
  max-width: 640rpx;
  width: calc(100% - 120rpx);
  display: flex;
  flex-direction: column;
  align-items: stretch;
  gap: 24rpx;
}

.equipment-tooltip__card {
  position: relative;
  width: 100%;
  padding: 24rpx 36rpx 36rpx;
  border-radius: 28rpx;
  background: rgba(18, 30, 74, 0.92);
  border: 1rpx solid rgba(102, 130, 238, 0.46);
  box-shadow: 0 24rpx 48rpx rgba(6, 10, 32, 0.55);
  display: flex;
  flex-direction: column;
  gap: 20rpx;
  box-sizing: border-box;
}

.equipment-tooltip__header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 16rpx;
}

.equipment-tooltip__name {
  flex: 1;
  min-width: 0;
  font-size: 30rpx;
  font-weight: 700;
  color: #f5f8ff;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}

.equipment-tooltip__quality {
  font-size: 24rpx;
  color: rgba(214, 224, 255, 0.88);
}

.equipment-tooltip__meta {
  display: flex;
  flex-wrap: wrap;
  gap: 10rpx 16rpx;
  font-size: 22rpx;
  color: rgba(198, 210, 255, 0.85);
}

.equipment-tooltip__stats,
.equipment-tooltip__notes {
  font-size: 22rpx;
  color: rgba(204, 216, 255, 0.9);
  line-height: 1.6;
  display: flex;
  flex-direction: column;
  gap: 8rpx;
}

.equipment-tooltip__description {
  font-size: 22rpx;
  color: rgba(210, 222, 255, 0.92);
  line-height: 1.6;
}

.equipment-tooltip__actions {
  display: flex;
  flex-wrap: wrap;
  gap: 12rpx;
}

.equipment-tooltip__action-button {
  margin: 0;
}

.equipment-tooltip__action-row {
  display: flex;
  align-items: center;
  gap: 12rpx;
  flex-wrap: wrap;
}

.equipment-tooltip__equip {
  margin-top: 0;
}

.equipment-tooltip__enhance {
  margin-top: 0;
}

.equipment-tooltip__hint {
  font-size: 22rpx;
  color: rgba(195, 206, 255, 0.78);
}

.equipment-tooltip__card--secondary {
  background: rgba(18, 30, 74, 0.88);
  border-color: rgba(102, 130, 238, 0.32);
  box-shadow: 0 16rpx 36rpx rgba(6, 10, 32, 0.45);
}

.equipment-tooltip__compare-title {
  font-size: 24rpx;
  color: rgba(195, 206, 255, 0.82);
}

.attribute-tooltip {
  position: fixed;
  inset: 0;
  z-index: 998;
  display: flex;
  align-items: center;
  justify-content: center;
}

.attribute-tooltip__mask {
  position: absolute;
  inset: 0;
  background: rgba(5, 9, 28, 0.72);
}

.attribute-tooltip__container {
  position: relative;
  max-width: 620rpx;
  width: calc(100% - 120rpx);
  padding: 0 8rpx;
  box-sizing: border-box;
}

.attribute-tooltip__card {
  position: relative;
  background: rgba(18, 30, 74, 0.94);
  border: 1rpx solid rgba(102, 130, 238, 0.46);
  box-shadow: 0 24rpx 48rpx rgba(6, 10, 32, 0.55);
  border-radius: 30rpx;
  padding: 32rpx 36rpx;
  display: flex;
  flex-direction: column;
  gap: 24rpx;
  max-height: 70vh;
}

.attribute-tooltip__header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 24rpx;
}

.attribute-tooltip__title {
  flex: 1;
  min-width: 0;
  font-size: 32rpx;
  font-weight: 700;
  color: #f5f8ff;
}

.attribute-tooltip__close {
  flex-shrink: 0;
  width: 60rpx;
  height: 60rpx;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  background: rgba(47, 70, 158, 0.36);
  color: rgba(229, 236, 255, 0.88);
  font-size: 36rpx;
  line-height: 1;
  transition: background 0.2s ease, color 0.2s ease;
}

.attribute-tooltip__close--hover {
  background: rgba(86, 110, 210, 0.6);
  color: #f8faff;
}

.attribute-tooltip__content {
  flex: 1;
  max-height: calc(70vh - 120rpx);
}

.attribute-tooltip__paragraph {
  font-size: 26rpx;
  line-height: 1.7;
  color: rgba(210, 222, 255, 0.92);
  margin-bottom: 18rpx;
}

.attribute-tooltip__paragraph:last-child {
  margin-bottom: 0;
}

.skill-slots {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(320rpx, 1fr));
  gap: 20rpx;
  margin-bottom: 24rpx;
}

.skill-slot-card {
  background: rgba(23, 37, 90, 0.85);
  border-radius: 24rpx;
  border: 1rpx solid rgba(93, 119, 214, 0.32);
  padding: 20rpx;
  display: flex;
  flex-direction: column;
  gap: 16rpx;
  min-height: 180rpx;
}

.skill-slot-card--empty {
  justify-content: center;
  align-items: center;
  text-align: center;
}

.skill-slot-card__header {
  display: flex;
  align-items: center;
  justify-content: space-between;
}

.slot-title {
  font-size: 24rpx;
  color: rgba(180, 195, 250, 0.78);
  font-weight: 500;
}

.skill-slot-card__action {
  margin-left: auto;
  flex-shrink: 0;
}

.skill-slot-card__body {
  display: flex;
  flex-direction: column;
  gap: 12rpx;
  padding-top: 4rpx;
}

.skill-slot-card__name {
  display: flex;
  align-items: baseline;
  gap: 8rpx;
  font-size: 28rpx;
  font-weight: 700;
  color: #f1f4ff;
}

.skill-slot-card__name-text {
  flex: 1;
  min-width: 0;
  overflow: hidden;
  white-space: nowrap;
  text-overflow: ellipsis;
}

.skill-slot-card__level {
  font-size: 22rpx;
  color: rgba(198, 210, 255, 0.82);
}

.skill-slot-card__tags {
  display: flex;
  flex-wrap: wrap;
  gap: 8rpx;
}

.skill-tag {
  padding: 4rpx 12rpx;
  border-radius: 12rpx;
  background: rgba(67, 94, 180, 0.32);
  color: rgba(198, 210, 255, 0.88);
  font-size: 20rpx;
}

.skill-tag--muted {
  background: rgba(55, 71, 130, 0.26);
  color: rgba(188, 202, 255, 0.7);
}

.skill-slot-card__meta {
  font-size: 22rpx;
  color: rgba(188, 202, 255, 0.82);
}

.skill-slot-card__highlights {
  font-size: 22rpx;
  color: rgba(177, 193, 247, 0.78);
  display: flex;
  flex-direction: column;
  gap: 6rpx;
  line-height: 1.6;
}

.skill-slot-card__highlight-line {
  display: block;
}

.slot-empty {
  font-size: 24rpx;
  color: rgba(172, 186, 240, 0.72);
}

.skill-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(320rpx, 1fr));
  gap: 20rpx;
}

.skill-card {
  position: relative;
  border: 1rpx solid rgba(93, 119, 214, 0.3);
  border-radius: 24rpx;
  padding: 20rpx 24rpx;
  background: rgba(18, 30, 72, 0.88);
  color: #f1f4ff;
  display: flex;
  flex-direction: column;
  gap: 12rpx;
}

.skill-card__highlights {
  font-size: 22rpx;
  color: rgba(177, 193, 247, 0.78);
  display: flex;
  flex-direction: column;
  gap: 6rpx;
  line-height: 1.6;
}

.skill-card__highlight-line {
  display: block;
}

.skill-card__badge {
  position: absolute;
  top: 16rpx;
  right: 16rpx;
  padding: 6rpx 16rpx;
  border-radius: 20rpx;
  background: rgba(86, 110, 210, 0.4);
  color: rgba(214, 224, 255, 0.9);
  font-size: 20rpx;
}

.skill-card__header {
  display: flex;
  align-items: baseline;
  justify-content: space-between;
  gap: 12rpx;
}

.skill-card__name {
  flex: 1;
  font-size: 26rpx;
  font-weight: 600;
  color: #f1f4ff;
  overflow: hidden;
  white-space: nowrap;
  text-overflow: ellipsis;
}

.skill-card__level {
  font-size: 22rpx;
  color: rgba(198, 210, 255, 0.82);
}

.enhancement-modal {
  position: fixed;
  inset: 0;
  z-index: 1600;
  display: flex;
  align-items: center;
  justify-content: center;
}

.enhancement-modal__mask {
  position: absolute;
  inset: 0;
  background: rgba(6, 12, 32, 0.72);
}

.enhancement-modal__panel {
  position: relative;
  width: 720rpx;
  max-width: 94vw;
  max-height: 82vh;
  background: rgba(16, 26, 64, 0.96);
  border-radius: 32rpx;
  border: 1rpx solid rgba(98, 130, 236, 0.45);
  box-shadow: 0 28rpx 60rpx rgba(6, 10, 32, 0.58);
  padding: 32rpx;
  box-sizing: border-box;
  display: flex;
  flex-direction: column;
  gap: 0rpx;
  overflow-y: auto;
}

.enhancement-modal__header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 24rpx;
}

.enhancement-modal__title {
  font-size: 36rpx;
  font-weight: 700;
  color: #f5f8ff;
}

.enhancement-modal__close {
  width: 60rpx;
  height: 60rpx;
  border-radius: 999rpx;
  background: rgba(54, 76, 170, 0.32);
  color: rgba(226, 236, 255, 0.86);
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 34rpx;
  transition: background 0.2s ease, color 0.2s ease;
}

.enhancement-modal__close--hover {
  background: rgba(84, 110, 214, 0.6);
  color: #f8faff;
}

.enhancement-modal__item-label {
  font-size: 24rpx;
  color: rgba(194, 206, 255, 0.78);
  font-weight: 500;
  text-align: center;
}

.enhancement-modal__item {
  display: flex;
  align-items: center;
  gap: 18rpx;
}

.enhancement-modal__item--stacked {
  flex-direction: column;
  gap: 20rpx;
}

.enhancement-modal__icon-frame {
  width: 100rpx;
  height: 100rpx;
  border-radius: 18rpx;
  border: 2rpx solid rgba(118, 146, 250, 0.5);
  background: rgba(12, 20, 52, 0.92);
  display: flex;
  align-items: center;
  justify-content: center;
  overflow: hidden;
}

.enhancement-modal__icon-frame--selectable {
  border-style: dashed;
}

.enhancement-modal__icon-frame--active {
  border-color: rgba(90, 212, 160, 0.86);
  box-shadow: 0 0 0 4rpx rgba(90, 212, 160, 0.2);
}

.enhancement-modal__icon {
  width: 100%;
  height: 100%;
  border-radius: inherit;
}

.enhancement-modal__icon--placeholder {
  font-size: 24rpx;
  color: rgba(188, 204, 255, 0.72);
  text-align: center;
  padding: 12rpx;
  line-height: 1.4;
}

.enhancement-modal__item-info {
  display: flex;
  flex-direction: column;
  gap: 6rpx;
  min-width: 0;
}

.enhancement-modal__item-info--center {
  align-items: center;
  text-align: center;
}

.enhancement-modal__item-name {
  font-size: 28rpx;
  font-weight: 600;
  color: #f1f4ff;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}

.enhancement-modal__item-meta {
  font-size: 22rpx;
  color: rgba(194, 206, 255, 0.78);
}

.enhancement-modal__item-placeholder {
  font-size: 22rpx;
  color: rgba(194, 206, 255, 0.66);
  line-height: 1.5;
}

.enhancement-modal__section-title {
  font-size: 26rpx;
  font-weight: 600;
  color: rgba(214, 224, 255, 0.9);
}

.enhancement-modal__result {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 12rpx;
}

.enhancement-modal__success-rate {
  font-size: 36rpx;
  font-weight: 700;
  color: #f7d27b;
  text-shadow: 0 6rpx 18rpx rgba(247, 210, 123, 0.32);
}

.enhancement-modal__result-icon-frame {
  width: 100rpx;
  height: 100rpx;
  border-radius: 20rpx;
  border: 2rpx solid rgba(118, 146, 250, 0.5);
  background: rgba(12, 20, 52, 0.92);
  display: flex;
  align-items: center;
  justify-content: center;
  overflow: hidden;
  box-shadow: 0 14rpx 30rpx rgba(32, 52, 120, 0.42);
}

.enhancement-modal__result-name {
  font-size: 28rpx;
  font-weight: 600;
  color: #f1f4ff;
}

.enhancement-modal__result-meta {
  font-size: 24rpx;
  color: rgba(198, 210, 255, 0.82);
}

.enhancement-modal__result-attributes {
  display: flex;
  flex-direction: column;
  gap: 6rpx;
  align-items: center;
  text-align: center;
}

.enhancement-modal__result-attribute {
  font-size: 24rpx;
  color: rgba(214, 224, 255, 0.9);
}

.enhancement-modal__result-arrow {
  display: flex;
  justify-content: center;
  margin:0rpx 0 -30rpx 0;
}

.enhancement-modal__arrow-icon {
  font-size: 42rpx;
  color: rgba(118, 212, 232, 0.9);
  animation: enhancement-modal-arrow-bounce 1.4s ease-in-out infinite;
}

@keyframes enhancement-modal-arrow-bounce {
  0%,
  100% {
    transform: translateY(0);
  }
  50% {
    transform: translateY(-10rpx);
  }
}

.enhancement-modal__pair {
  display: flex;
  gap: 28rpx;
  background: rgba(24, 38, 90, 0.58);
  border: 1rpx solid rgba(94, 126, 228, 0.32);
  border-radius: 24rpx;
  padding: 18rpx 32rpx;
  align-items: stretch;
  flex-wrap: nowrap;
  justify-content: space-between;
}

.enhancement-modal__pair-side {
  flex: 1 1 0;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 18rpx;
  min-width: 0;
}

.enhancement-modal__pair-divider {
  width: 2rpx;
  background: rgba(94, 126, 228, 0.32);
  border-radius: 2rpx;
  align-self: stretch;
}

.enhancement-modal__materials {
  display: flex;
  flex-direction: column;
  gap: 16rpx;
  margin-top:20rpx;
}

.enhancement-modal__material-list {
  display: flex;
  flex-wrap: wrap;
  gap: 16rpx;
  justify-content: flex-start;
}

.enhancement-modal__material-item {
  background: rgba(24, 38, 88, 0.48);
  border: 1rpx solid rgba(90, 118, 224, 0.28);
  border-radius: 22rpx;
  padding: 16rpx;
  display: flex;
  flex-direction: column;
  gap: 10rpx;
  align-items: center;
  transition: border-color 0.2s ease, box-shadow 0.2s ease;
}

.enhancement-modal__material-item--active {
  border-color: rgba(90, 212, 160, 0.86);
  box-shadow: 0 10rpx 24rpx rgba(90, 212, 160, 0.32);
}

.enhancement-modal__material-icon {
  width: 100rpx;
  height: 100rpx;
  border-radius: 18rpx;
  border: 2rpx solid rgba(118, 146, 250, 0.42);
  background: rgba(12, 20, 52, 0.92);
  display: flex;
  align-items: center;
  justify-content: center;
  overflow: hidden;
}

.enhancement-modal__material-name {
  font-size: 24rpx;
  color: #f1f4ff;
  text-align: center;
  line-height: 1.3;
}

.enhancement-modal__material-meta {
  font-size: 22rpx;
  color: rgba(198, 210, 255, 0.78);
}

.enhancement-modal__actions {
  display: flex;
  justify-content: flex-end;
  gap: 16rpx;
  flex-wrap: wrap;
}

.enhancement-modal__action {
  min-width: 160rpx;
}

.enhancement-modal__hint {
  font-size: 22rpx;
  color: rgba(194, 206, 255, 0.72);
  text-align: right;
  margin-top: 16rpx;
}

.enhancement-modal__error {
  padding: 24rpx;
  border-radius: 20rpx;
  background: rgba(250, 81, 81, 0.12);
  border: 1rpx solid rgba(250, 81, 81, 0.4);
  color: rgba(255, 186, 186, 0.9);
  font-size: 24rpx;
  line-height: 1.6;
}

.enhancement-modal__loading {
  font-size: 26rpx;
  color: rgba(206, 216, 255, 0.88);
  text-align: center;
  padding: 40rpx 0;
}

.enhancement-modal__empty {
  font-size: 24rpx;
  color: rgba(194, 206, 255, 0.72);
  line-height: 1.6;
  text-align: center;
  padding: 20rpx 0;
}

.skill-card__tags {
  display: flex;
  flex-wrap: wrap;
  gap: 8rpx;
}

.skill-card__tags--secondary {
  margin-top: -4rpx;
}

.skill-card__meta {
  font-size: 22rpx;
  color: rgba(184, 197, 247, 0.74);
}

.skill-card__footer {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 12rpx;
  font-size: 22rpx;
  color: rgba(184, 197, 247, 0.74);
}

.skill-card__time {
  flex: 1;
  min-width: 0;
  text-align: right;
  font-size: 22rpx;
  color: rgba(184, 197, 247, 0.72);
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}

.skill-card--hover {
  background: rgba(27, 43, 102, 0.92) !important;
  border-color: rgba(113, 140, 255, 0.42) !important;
}

.skill-modal {
  position: fixed;
  inset: 0;
  z-index: 1200;
  display: flex;
  align-items: center;
  justify-content: center;
}

.skill-modal__mask {
  position: absolute;
  inset: 0;
  background: rgba(5, 9, 28, 0.72);
}

.skill-modal__card {
  position: relative;
  width: calc(100% - 120rpx);
  max-width: 660rpx;
  max-height: 80vh;
  padding: 32rpx 36rpx 40rpx;
  border-radius: 28rpx;
  background: rgba(18, 30, 74, 0.94);
  border: 1rpx solid rgba(102, 130, 238, 0.46);
  box-shadow: 0 24rpx 48rpx rgba(6, 10, 32, 0.55);
  box-sizing: border-box;
  display: flex;
  flex-direction: column;
  gap: 12rpx;
  color: #f1f4ff;
  overflow-x: hidden;
  overflow-y: auto;
}

.skill-modal__card::after {
  content: '';
  position: absolute;
  inset: 0;
  border-radius: inherit;
  pointer-events: none;
  box-shadow: inset 0 0 0 1rpx rgba(72, 97, 190, 0.25);
}

.skill-modal__header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 16rpx;
}

.skill-modal__title {
  flex: 1;
  min-width: 0;
  font-size: 32rpx;
  font-weight: 700;
  color: #f1f4ff;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}

.skill-modal__level {
  font-size: 24rpx;
  color: rgba(214, 224, 255, 0.88);
}

.skill-modal__tags {
  display: flex;
  flex-wrap: wrap;
  gap: 8rpx;
  margin-bottom: 4rpx;
}

.skill-modal__line {
  font-size: 24rpx;
  color: rgba(188, 202, 255, 0.86);
}

.skill-modal__line--muted {
  color: rgba(175, 191, 244, 0.7);
}

.skill-modal__desc {
  font-size: 24rpx;
  color: rgba(198, 210, 255, 0.88);
  line-height: 1.6;
  margin-top: 8rpx;
}

.skill-modal__highlights {
  display: flex;
  flex-direction: column;
  gap: 8rpx;
  font-size: 22rpx;
  color: rgba(177, 193, 247, 0.82);
}

.skill-modal__actions {
  margin-top: 12rpx;
  display: flex;
  flex-wrap: wrap;
  gap: 12rpx;
  justify-content: flex-end;
}

.history-item {
  padding: 16rpx 0;
  border-bottom: 1rpx dashed rgba(93, 119, 214, 0.26);
}

.history-item:last-child {
  border-bottom: none;
}

.history-title {
  font-size: 24rpx;
  font-weight: 600;
  color: rgba(203, 214, 255, 0.88);
}

.history-desc {
  font-size: 22rpx;
  color: rgba(177, 193, 247, 0.76);
  margin-top: 6rpx;
}

.empty-card {
  background: transparent;
  text-align: center;
  margin: 16rpx 0 32rpx;
}

.empty-tip {
  text-align: center;
  color: rgba(165, 182, 238, 0.75);
  font-size: 24rpx;
  padding: 12rpx 0;
}
